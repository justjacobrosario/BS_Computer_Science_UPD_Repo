#include <stdio.h>


int c[3] = {10, 100, 5};
int v[3] = {1, 1000, 7};

int max_value(int w, int n, const int *c, const int *v) {
    int res = 0;

    for (int i = 0; i < n; i++) {
        if (c[i] <= w) {
            new = w - c[i] + v[i]
        }
    }
}

max_value(10, 2, c, v);
