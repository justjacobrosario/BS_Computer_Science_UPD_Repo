#include <stdio.h>
#include <stdlib.h>

/*
int main() {
	int id;
	char msg[67];

	while (1) {
		printf("Input id: ");
		scanf("%d", &id);

		printf("Input message: ");
		scanf("%s", msg);

		printf("ID: %d, MSG: %s\n", id, msg);
	}
}
*/

int main() {
	int id;
	char msg[67];

	while (1) {
		printf("Input id: ");
		scanf("%d", &id);

		printf("Input message: ");
		scanf("%s", msg);

		printf("ID: %d, MSG: %s\n", id, msg);
	}
}


