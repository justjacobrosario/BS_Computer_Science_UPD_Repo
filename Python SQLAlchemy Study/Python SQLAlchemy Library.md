## 1. Why SQLAlchemy
1. Allows Object Relational Mapping (ORM) which interacts the database using python objects.
2. Easier for databases to manipulate (since we do not need to manually type SQL command strings)

## 2. Installing necessary libraries and software

:  to install sqlalchemy `pip install sqlalchemy` in the terminal

: to install sqlite, refer to this tutorial https://youtu.be/FsiyOAkC0P4

## 3. Creating an Database Engine

: in the python file, we have to create a database engine to interpret and link python code to an actual database.

```python
from sql
```
