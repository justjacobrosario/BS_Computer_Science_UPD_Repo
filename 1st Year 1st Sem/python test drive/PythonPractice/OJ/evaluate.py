def evaluate(e, x):
    assigned = e.replace("x", str(x))
    if #base case:
        #blabla
    else:
        #find i of first *, make int(assigned[:i]), 
        index_times = assigned.index("*")
        current = int(assigned[:index_times]) *  


print(evaluate("x * x", 5))